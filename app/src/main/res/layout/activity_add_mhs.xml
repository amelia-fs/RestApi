<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".AddMhs"
    android:orientation="vertical"
    android:gravity="center_vertical"
    android:background="@drawable/bg">
    <TextView
        style="@style/tv_form"
        android:text="Nama Mahasiswa"/>
    <EditText
        android:id="@+id/et_add_nama"
        style="@style/edit_form"
        android:hint="Name Here"/>
    <TextView
        style="@style/tv_form"
        android:text="Jurusan Mahasiswa"/>
    <EditText
        android:id="@+id/et_add_jurusan"
        style="@style/edit_form"
        android:hint="Major Here!!"/>
    <TextView
        style="@style/tv_form"
        android:text="Telepon Mahasiswa"/>
    <EditText
        android:id="@+id/et_add_tlp"
        style="@style/edit_form"
        android:hint="Telephone Here!!"/>
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center">
        <Button
            android:id="@+id/btn_add_submit"
            android:text="Submit"
            style="@style/btn_form"/>
        <Button
            android:id="@+id/btn_add_cancel"
            android:text="Cancel"
            style="@style/btn_form"/>
    </LinearLayout>
</LinearLayout>
